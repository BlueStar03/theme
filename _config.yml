title: BlueStar theme
author: zbule
description: test theme for Jekyll

toc-include-h1: true

logo: "/assets/images/logo.svg"

defaults:
  # everything (pages, posts, collection items) in the site
  - scope:
      path: ""       # all files
    values:
      layout: default

 